module SignatureHelpTest {
  export class Calc {
    /**
     *
     * @param a A operand.
     * @param b Another operand.
     *
     **/
    add(a: number, b: number): number {
      return a + b;
    }
  }

  var calc = new Calc();
  calc.add( 
}

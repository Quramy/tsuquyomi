box: node
build:
  steps:
    - npm-install
    - script:
        name: install deps
        code: |
          apt-get update -y
          apt-get install -y vim
    - script:
        name: echo nodejs information
        code: |
          vim --version
